<!--components/tab-bar/index.wxml-->
<van-tabbar active="{{ active }}" bind:change="onChange">
  <van-tabbar-item wx:for="{{navs}}" wx:key="index" name="{{item.name}}" icon="{{item.icon}}">
    {{item.label}}
  </van-tabbar-item>
</van-tabbar>